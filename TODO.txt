# ToDo for linLIR:

Dokumentation anpassen
- plot.s.linlir -> para ->
.option polygon: minimal geändert, sodass eine mögliche Offenheit bei 0 klarer sichtbar wird.
.p.cex für Steuerung der Größe der LRM-Punkte eingefügt
.pl.border hinzugefügt
- plot.idf/ plot.s.linlir: linebreaks für plot(... ) 
- alle: Referenzen aktualisieren!


- bei Gelegenheit hinzufügen:
Option für Datenplot (plot.idf), die auf polygon() basiert, wo jeder Datenpunkt als halbtransparentes Polygon (ohne Rand!) gezeichnet wird

